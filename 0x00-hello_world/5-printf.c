#include <stdio.h>
/**
 * main - Prints a string using printf to standard output
 * Return: 0 if successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
