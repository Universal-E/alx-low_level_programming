#include <stdio.h>

/**
 * main - Prints all single base 10 digits from 0
 * Return: 0 (success)
 */

int main(void)

{
	int a;

	for (a = 0; a < 10; a++)

	{
		printf("%d", a);
	}

	putchar('\n');
	return (0);
}
